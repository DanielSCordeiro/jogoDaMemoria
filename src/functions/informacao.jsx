export function setInfo(info, cartasViradas, paresFormados, setInformacao) {
  // atraso no carregamento para dar tempo dos efeitos visuais acontecerem
  setTimeout(() => {
    // setInformacao(prev => ({...prev, info: info}))
  }, 600)
}